startCommand:
  type: http
  configSchema:
    type: object
    properties:
      url:
        type: string
        default: "https://go.coremodels.io/mcp"
        description: "CoreModels MCP server endpoint"
    required: []
